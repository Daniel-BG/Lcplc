TO DO

--test big module
	*--test alpha calc
		*--test alpha finder

	--test nthband predictor

	--test error calc
		--test error mapper
		--test quantizer
		--test dequantizer
		--test sliding acc
		--test kjcalc


	--test next xhat precalc

	--test coder
		--test exp golomb
		--test golomb


Things done (for later consideration)
	1) 
		Positive number shift divides by 2**x and rounds towards zero
		Negative number shift divides by 2**x and rounds towards -infty (not towards zero)
	2) Options for shifts of negative numbers:
		2.1) Divider (twenty some latency plus all intermediate registers, plus DSPs, etc)
		2.2) Custom (smaller in registers)
		2.3) None, just shift (slight loss of precision)

		2.3 is the similar in compression efficiency (even better in tested cases (545211 vs 546821))
		so its better to have it
